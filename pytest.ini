[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# 环境约束（D1 实测）：本沙箱中带 mode=0o700 的 mkdir 会使目录不可访问，
# 故禁用 cacheprovider（不写 .pytest_cache）；用例一律不用 tmp_path，
# BASE 沙箱由 tests/conftest.py 自建（普通 mkdir + uuid + rmtree）。
addopts = -p no:cacheprovider
