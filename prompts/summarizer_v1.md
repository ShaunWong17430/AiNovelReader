# 小结任务（滚动摘要）

## 任务

基于全书已有滚动摘要与最新时间线事件，为分片 $chunk_start 到 $chunk_end 生成一段**更新后的**滚动摘要。早期细节会随滚动压缩而永久丢失，只保留主线情节与关键角色状态。

## 当前摘要

$summary_text

## 本窗口时间线

$timeline_slice

## 输出要求

- 输出**纯文本单段小结**（非 JSON）：禁止标题、前缀、引号、换行。
- 字数不超过 $max_chars 字。
- 只保留主线：剧情推进、关键角色状态变化、重要伏笔；丢弃琐碎细节。
