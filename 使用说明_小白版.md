# 使用说明（小白版）

> 给不会编程的你。跟着做就行，不用懂原理。
> 先试**图形界面（GUI）**，全程点鼠标；嫌麻烦的旧命令行方式放在文末「命令行备选」。
> 遇到报错先看文末「常见问题」，实在不行把屏幕上显示的红字截图发给会看的人。

## 1. 这是干什么的？

一个「AI 帮你读书做笔记」的小工具。你把小说的文字切成一小段一小段放进文件夹，
它会：

1. 一段一段读给 AI 听；
2. AI 读完回传「这一段发生了什么、有哪些人物」；
3. 工具自动整理成**读书笔记**和**情节时间线**（谁在哪章做了什么，一目了然）；
4. 每隔几章自动让 AI 写一段**前情小结**，方便你回忆前面讲了啥；
5. 全本读完自动结束。

**跑一半停电/断网/手滑关掉窗口？不要紧，重新打开再点一次「开始」，它会从上次停的
地方继续，不会重复花钱。**

---

## 2. 需要准备什么？

| 东西 | 说明 |
|---|---|
| 一台 Windows 电脑 | 程序已装好，不用你装任何东西 |
| 一个 AI 的 API Key | 相当于「充值卡」，按用量扣费（一本小书约几毛钱）。问给你装程序的人要 |
| 小说的文字稿 | 纯文字版（.txt），每章一个文件，或者一章切成几个文件 |

---

## 3. 三步开始使用（图形界面版）

### 第 1 步：放小说

1. 打开文件夹 `D:\data\dsh_wz1\novel_reading`；
2. 新建一个文件夹，名字就是书名（比如 `我的小说`）；
3. 在里面再建一个文件夹，叫 `chunks`；
4. 把小说文字按顺序放进去，文件名叫：
   - `part_001.txt`
   - `part_002.txt`
   - `part_003.txt`
   - ……（数字编号连起来，别跳号、别改名）

> 文件保存为「UTF-8」编码（记事本另存为时选这个）。每段别太长，**一个文件不要
> 超过 3 万“字”（去掉空格标点后）**，不然 AI 一次读不下。

### 第 2 步：打开程序

双击运行（或在命令行敲）：

```
D:\data\dsh_wz1\pyfordsh\python.exe gui_launcher.py
```

会弹出一个标题为「小说阅读自动化 · 控制台」的窗口。

### 第 3 步：按窗口里的按钮走

窗口从上到下分四块，照着做：

1. **选小说**：点「浏览…」，找到你第 1 步建的小说文件夹
   （`D:\data\dsh_wz1\novel_reading\我的小说`，就是**带 chunks 文件夹的那个**），
   选中它。旁边会显示 `base: … · 小说: 我的小说` 就对了。
   （如果你选的是 `novel_reading` 这个大文件夹，它会提示你进到具体小说目录里选。）
2. **填配置**：窗口中间有两栏——左边「阅读模型」，右边「小结模型」。
   每栏填三个东西：
   - **接口 URL**：AI 服务商的地址（一般不用改，装程序的人会给）
   - **模型名称**：比如左边 `Qwen`（本地）、右边 `deepseek-v4-flash-0731`（远程）（装程序的人会给）
   - **API Key**：你的钥匙（输入后会自动加密保存，窗口里显示成小圆点）
   两栏可以填不一样的——左边管「读小说」，右边管「写小结」，互不干扰。
3. **点「保存配置」**：把上面的设置记下来（钥匙是加密存的，别人看不到）。
4. **点「初始化」**：检查你的分片有没有问题，生成「档案」。看到日志出现
   `初始化完成` 之类的话就行。
5. **点「开始」**：它开始读书了！下面日志框会一行行刷新，右边「元数据」表里的
   「已读片数」会慢慢变大。
6. **想停下**：点「停止」（相当于按 Ctrl+C），它读完当前这一段就停。下次再点
   「开始」就从停的地方接着读。

**读到什么时候算完？** 右边元数据表里「状态」变成**已完成**（绿色字），
「已读片数」=「总片数」，就是读完了。

去 `D:\data\dsh_wz1\novel_reading\我的小说\` 里看成果：

- `notes` 文件夹 → 读书笔记（每批一份）
- `plot_timeline.md` → 情节时间线
- `summary.md` → 全书小结

---

## 4. 界面上的东西都是啥？（认字表）

| 窗口里的名字 | 意思 |
|---|---|
| 状态 | 待初始化 / 运行中 / 已完成 / 出错 |
| 总片数 / 已读片数 | 一共多少段 / 已经读完多少段 |
| 批大小 | 一次一起处理几段（默认 5） |
| 时间线窗口 | 读每段时往前带几段前情（默认 10 段）；也决定隔几段小结一次（默认每 11 段一次） |
| 小结上限字数 | 前情小结最多写多少字（默认 5000） |
| 单片上限字数 | 一段最多多少字（默认 2 万，超了会报错） |
| 调用次数 / 输入 tokens / 输出 tokens | 花了 AI 多少次调用、多少字（算钱用的） |
| 错误信息 | 出错时会红字显示原因 |

右下的「日志」框，是把程序干活的每一句话都列出来，看着密密麻麻是正常的。

---

## 5. 可以改哪些参数？（按需，不改也能用）

**图形界面不能直接改参数**——参数在它第一次初始化时定死了。想改的话，在
`D:\data\dsh_wz1\novel_reading\我的小说\metadata.json` 里改（用记事本打开），
**只准改这四个**：

| 字段 | 默认值 | 能改什么 | 什么时候改 |
|---|---|---|---|
| `batch_size` | 5 | 一次一起读几段 | 想多读几段省次数（调大到 10），或怕一次太多（调小到 3） |
| `timeline_window` | 10 | 读每段时往前带几段前情；同时决定隔几段写一次前情小结（隔 11 段） | 想更频繁小结、或想让 AI 记得更多近情就调小（如 5） |
| `summary_max` | 5000 | 小结最多多少字 | 想小结更详细就调大（如 8000） |
| `chunk_max_chars` | 20000 | 每段最多多少字（不能超 3 万） | 你的 AI 模型小、老报错就调小（如 10000） |

> 除了这四个，`metadata.json` 里其它字段**一律别碰**。

改的规矩（非常重要）：

1. **先确认没在跑**：状态不是「运行中」，点着「开始」时千万别改；
2. **先备份**：把 `metadata.json` 复制一份改名 `metadata.json.bak` 放旁边；
3. 只改上面那四个数字，其它一个字都别动；
4. 改完保存，再点「开始」继续；**已经读完的部分不会重读**，新参数只管后面。

> ⚠️ 改坏了 → 整本书会「出错」且救起来很麻烦。拿不准就只改备份、别改原件。

**在图形界面里能改的**：中间的「接口 URL」「模型名称」「API Key」——改完点
「保存配置」，下次「开始」就生效（已读部分不重读）。

---

## 6. 红线警告（千万别做）

1. **别在它跑的时候**去动 `notes`、`plot_timeline.md`、`summary.md` 这些文件；
2. **别开两个窗口同时跑同一本书**（图形界面 + 命令行也不行，会打架）；
3. **别手动改** `metadata.json`、`.batch`、`.rollback` 文件夹里的东西——除了
   第 5 节说的那四个参数，改坏了只能找会的人救；
4. **别把 `llm_payload.log` 发给别人**（只有调试时才出现，里面是小说原文，有版权）；
5. **别删 `chunks` 里的原文**，它只读不碰，删了它反而会报错；
6. **别把钥匙告诉别人**：钥匙是加密存在电脑里的，但你自己也别到处发。

---

## 7. 常见问题（遇到红字先看这里）

| 现象 | 怎么办 |
|---|---|
| 打开窗口，元数据表全是「—」 | 正常，还没选小说目录（第 3 步第 1 点），或还没「初始化」 |
| 点「开始」没反应 | 看顶部状态栏提示：多半是没选对目录——要选**带 chunks 的那个小说文件夹**，不是库根目录 |
| 说「缺少 api_key」 | 第 3 步第 2 点的钥匙没填，或填完没点「保存配置」 |
| 说「chunks 不存在/为空」 | 第 1 步没做对：检查 `D:\data\dsh_wz1\novel_reading\我的小说\chunks\` 里有没有 `part_001.txt` |
| 说「分片命名缺口/编码失败」 | 文件没按 `part_001`、`part_002` 连续编号，或不是 UTF-8 编码 |
| 跑一半自己停了，状态是「出错」 | ① 是你自己点的「停止」→ 直接再点「开始」即可（从断点继续）；② 是它自己报错停的（状态=出错）→ 先看元数据表「错误信息」那行，多数是 AI 暂时不可用；先等一会儿，再点「开始」试试。反复报同一个错 → 把「错误信息」原文发给装程序的人 |
| 一直报「字数超限/内容太长」 | 把每段再切小一点（第 1 步），或把 `chunk_max_chars` 调小（第 5 节） |
| 想重新从头读一遍 | 把 `D:\data\dsh_wz1\novel_reading\我的小说` 整个删掉，重新按第 1、3 步来（旧成果也没了，慎用） |
| 窗口关了/断电了，还能接着读吗 | 能。重新打开程序，选同一个小说目录，点「开始」，从断点继续 |
| API Key 输入框是空的，要重填吗 | 不用。只要之前点过「保存配置」，钥匙已加密存好；输入框留空点「开始」会自动用已存的钥匙 |

---

## 8. 命令行备选（会复制粘贴就行）

不想用图形界面的话，也可以像以前一样用命令行。打开命令行（按 `Win` 键，输入
`cmd`，回车），把下面这行**整段复制粘贴**进去，回车：

```bat
cd /d D:\data\dsh_wz1\novel_skill
D:\data\dsh_wz1\pyfordsh\python.exe run.py run --novel 我的小说
```

（把 `我的小说` 换成你的书名文件夹名）

经常用到的几条（复制粘贴改书名就行）：

```bat
:: 查看进度（随时可看）
D:\data\dsh_wz1\pyfordsh\python.exe run.py status --novel 我的小说

:: 先试跑一遍、看它打算做什么（不花钱）
D:\data\dsh_wz1\pyfordsh\python.exe run.py run --novel 我的小说 --dry-run

:: 读到一半停了，想接着读（直接重跑这一条即可）
D:\data\dsh_wz1\pyfordsh\python.exe run.py run --novel 我的小说

:: 想中途歇一下：在窗口里按 Ctrl+C，它会读完当前这一小段再停
```

命令行方式填钥匙（二选一）：

```bat
:: 方式一：改 config.json 里 "api_key": null → "api_key": "sk-你的钥匙"
:: 方式二：让装程序的人把钥匙写进电脑的环境变量 NOVEL_LLM_API_KEY
```

---

## 9. 最简备忘

```
放小说（chunks\part_001.txt…）→ 开程序 → 选目录 → 填钥匙 → 保存 → 初始化 → 开始

跑一半自己按停了 → 再点「开始」（或重跑同一条命令）
自己报错停了（状态=出错）→ 看「错误信息」红字；多数等一会儿再「开始」就行
想改参数 → 等它停了，备份后只改 metadata.json 里那四个数字
看成果 → 小说文件夹里的 notes / plot_timeline.md / summary.md
```
